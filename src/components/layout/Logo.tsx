export function Logo() {
  return (
    <div className="font-headline text-2xl font-bold tracking-tight">
      <span style={{ color: 'hsl(var(--primary))' }}>Sy</span>
      <span style={{ color: 'hsl(var(--accent))' }}>nr</span>
    </div>
  );
}
